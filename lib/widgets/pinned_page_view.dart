import 'package:flutter/material.dart';

class PinnedPageView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Placeholder(
      fallbackHeight: 800,
    );
  }
}
