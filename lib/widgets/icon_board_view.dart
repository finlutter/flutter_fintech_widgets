import 'package:flutter/material.dart';

class IconBoardView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Placeholder(
      fallbackHeight: 248,
    );
  }
}
